name: env demo

on:
  #push
  workflow_dispatch
jobs:
    build:
        runs-on: ubuntu-latest

        steps:
          - name: Install
            env:
              APP_ENV: Demo
              DB_PASSWORD: ${{secrets.DB_PASSWORD}}
            run: echo "environment $APP_ENV and $DB_PASSWORD"

 

