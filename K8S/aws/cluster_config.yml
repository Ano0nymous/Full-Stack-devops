# cluster-config.yaml
apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: my-clusters
  region: us-east-2
  version: "1.34"

managedNodeGroups:
- name: ng-1
  instanceType: t3.medium
  desiredCapacity: 1
  minSize: 1
  maxSize: 2